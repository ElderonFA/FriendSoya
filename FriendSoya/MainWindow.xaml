<Window x:Class="FriendSoya.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FriendSoya" 
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Width="416" 
        Height="416"
        Opacity="0.9">
    
    <Window.Resources>
        <Style x:Key="ContextMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="4">
                            <ContentPresenter Content="{TemplateBinding Content}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3A4FC3F7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid x:Name="MainGrid" MouseDown="Grid_MouseDown" MouseUp="Grid_MouseUp" MouseRightButtonDown="Grid_MouseRightButtonDown">
        <Image x:Name="gjinImage" Source="/Images/gjinIdle.png"></Image>

        <Ellipse x:Name="heart" RenderTransformOrigin="0.5,0.5" Margin="150,198,151,98">
            <Ellipse.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="heartScaleTransform" ScaleX="1" ScaleY="1"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform X="0.5" Y="0.5"/>
                </TransformGroup>
            </Ellipse.RenderTransform>
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop x:Name="heartGradientColor" Color="#7F0000FF" Offset="0"/>
                    <GradientStop Color="#1F0000FF" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
            <Ellipse.Effect>
                <BlurEffect Radius="25"/>
            </Ellipse.Effect>
        </Ellipse>

        <!-- Popup для контекстного меню -->
        <Popup x:Name="ContextPopup" 
               StaysOpen="True"
               Placement="Mouse"
               AllowsTransparency="True"
               PopupAnimation="Fade" Closed="ContextPopup_Closed">

            <!-- Содержимое Popup'а -->
            <Border x:Name="MenuBorder"
            Background="#E6101A2E"
            BorderBrush="#7F4FC3F7"
            BorderThickness="1"
            CornerRadius="8">
                <StackPanel Margin="5">
                    <Button x:Name="ChatButton" Content="Открыть чат" 
                    Style="{StaticResource ContextMenuButtonStyle}"
                    Click="ContextChatButton_Click"/>
                    <Button x:Name="TopMostButton" Content="Обычное окно" 
                    Style="{StaticResource ContextMenuButtonStyle}"
                    Click="ContextTopMostButton_Click"/>
                    <Button x:Name="AboutButton" Content="О программе"
                    Style="{StaticResource ContextMenuButtonStyle}"        
                    Click="ContextAboutButton_Click"/>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>
</Window>